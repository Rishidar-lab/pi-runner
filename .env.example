# Pi Developer Portal API key (server-side only — never commit the real value).
# Get it in the Pi Browser: open pi://develop.pinet.com, register/select your app,
# and copy its API key here.
PI_API_KEY=

# Optional overrides
# PORT=3000
# PI_API_BASE=https://api.minepi.com/v2
# DATA_DIR=./server/data
