# Get this from the Pi Developer Portal:
# Open the Pi Browser, go to pi://develop.pinet.com, register/select your app,
# and copy its API key here. Never commit your real key.
PI_API_KEY=

# Optional: override the port (defaults to 3000)
# PORT=3000
